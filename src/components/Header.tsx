import React from "react";

function Header() {
  return (
    <header
      style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        backgroundColor: "paleturquoise",
      }}
    >
      <h2>Header</h2>
    </header>
  );
}

export default Header;
