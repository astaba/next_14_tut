import { Metadata } from "next";

export const metadata: Metadata = {
  title: {
    absolute: "About Codevolution",
  },
};

export default function About() {
  return <h1>(...) intercept About Page</h1>;
}
